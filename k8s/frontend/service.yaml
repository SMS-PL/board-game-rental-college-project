apiVersion: v1
kind: Service
metadata:
  name: rental-frontend
  namespace: rental-games
spec:
  selector:
    app: rental-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort

